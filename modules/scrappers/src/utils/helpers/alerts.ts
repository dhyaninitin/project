import SlackNotify from 'slack-notify';
import { slackConfig, zapier } from '../../config/config';
import Vehicles from '../../model/vehicle';
import moment from 'moment';
import Models from '../../model/model';


export const slackNotification = async (text: string) => {

}


export const newlyVehicleAdded = async(text:string) => {

}
